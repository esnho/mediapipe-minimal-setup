// comment these imports and related console.log() to see that the project by itself is working
import { FaceMesh } from '@mediapipe/face_mesh';
import { Camera } from '@mediapipe/camera_utils';
import { ControlPanel } from '@mediapipe/control_utils';
import { drawConnectors, FACEMESH_TESSELATION } from '@mediapipe/drawing_utils';

console.log(FaceMesh);
console.log(Camera);
console.log(ControlPanel);
console.log(drawConnectors);
console.log(FACEMESH_TESSELATION);

console.log("HELLO WORLD");